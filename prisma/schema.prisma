// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlserver"
  url      = env("MSSQL_URL")
}

model bank {
  id          Int           @id @default(autoincrement())
  bank_name   String        @db.VarChar(50)
  is_active   Boolean       @default(true)
  created_at  DateTime      @default(now()) @db.DateTime()
  updated_at  DateTime?     @db.DateTime()
  deleted_at  DateTime?     @db.DateTime()
  created_by  Int?
  updated_by  Int?
  deleted_by  Int?
  sales       sales[]
  vendor_bank vendor_bank[]
}

model city {
  id          Int           @id @default(autoincrement())
  province_id Int?
  city_name   String        @db.VarChar(255)
  created_at  DateTime      @default(now()) @db.DateTime()
  updated_at  DateTime?     @db.DateTime()
  deleted_at  DateTime?     @db.DateTime()
  created_by  Int?
  updated_by  Int?
  deleted_by  Int?
  members     members[]
  province    province?     @relation(fields: [province_id], references: [id])
  vendor_area vendor_area[]
}

model employee {
  id              Int        @id @default(autoincrement())
  position_id     Int?
  store_id        Int?
  user_id         Int?       @unique
  first_name      String     @db.VarChar(50)
  middle_name     String?    @db.VarChar(50)
  last_name       String     @db.VarChar(50)
  full_name       String     @db.VarChar(255)
  email           String     @db.VarChar(255)
  birth           DateTime   @db.DateTime()
  gender          String     @db.VarChar(50)
  nik             String     @db.VarChar(50)
  phone_number    String     @db.VarChar(50)
  whatsapp_number String     @db.VarChar(50)
  created_at      DateTime?  @default(now()) @db.DateTime()
  updated_at      DateTime?  @db.DateTime()
  deleted_at      DateTime?  @db.DateTime()
  created_by      Int?
  updated_by      Int?
  deleted_by      Int?
  positions       positions? @relation(fields: [position_id], references: [id])
  store           store?     @relation(fields: [store_id], references: [id])
  users           users?     @relation(fields: [user_id], references: [id])
}

model items {
  id              Int               @id @default(autoincrement())
  item_name       String            @db.VarChar(45)
  category_name   String
  created_at      DateTime?         @default(now()) @db.DateTime()
  updated_at      DateTime?         @db.DateTime()
  deleted_at      DateTime?         @db.DateTime()
  created_by      Int?
  updated_by      Int?
  deleted_by      Int?
  m_order_details m_order_details[]
  prices          prices[]
}

model logs {
  id          Int       @id @default(autoincrement())
  module_type String?   @db.VarChar(255)
  module_id   Int?
  issuer_type String?   @db.VarChar(255)
  issuer_id   Int?
  properties  String?   @db.Text
  created_at  DateTime? @default(now()) @db.DateTime()
}

model members {
  id              Int       @id @default(autoincrement())
  full_name       String    @db.VarChar(255)
  email           String    @unique() @db.VarChar(255)
  phone_number    String    @db.VarChar(20)
  whatsapp_number String    @db.VarChar(20)
  address_1       String    @db.VarChar(255)
  address_2       String?   @db.VarChar(255)
  city_id         Int?
  zip_code        String?   @db.VarChar(10)
  rating          Int?      @default(0)
  join_date       DateTime  @default(now()) @db.DateTime()
  join_location   Int?
  created_at      DateTime  @default(now()) @db.DateTime()
  updated_at      DateTime? @db.DateTime()
  deleted_at      DateTime? @db.DateTime()
  created_by      Int?
  updated_by      Int?
  deleted_by      Int?
  city            city?     @relation(fields: [city_id], references: [id])
  order           orders[]
}

model menus {
  id        Int    @id @default(autoincrement())
  parent_id Int?
  parent    menus? @relation("ChildMenus", fields: [parent_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  title     String  @db.VarChar(50)
  url       String  @db.VarChar(255)
  icon      String  @db.VarChar(50)
  is_active Boolean @default(true)

  created_at DateTime? @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()
  created_by Int?
  updated_by Int?
  deleted_by Int?

  childMenus menus[] @relation("ChildMenus") // Define the reverse relation for self-referencing

  permissions permissions[]
}

model positions {
  id            Int        @id @default(autoincrement())
  position_name String     @db.VarChar(50)
  is_active     Boolean    @default(true)
  created_at    DateTime?  @default(now()) @db.DateTime()
  updated_at    DateTime?  @db.DateTime()
  deleted_at    DateTime?  @db.DateTime()
  created_by    Int?
  updated_by    Int?
  deleted_by    Int?
  employee      employee[]
}

model province {
  id            Int       @id @default(autoincrement())
  province_name String
  is_active     Boolean   @default(true)
  created_at    DateTime  @default(now()) @db.DateTime()
  updated_at    DateTime? @db.DateTime()
  deleted_at    DateTime? @db.DateTime()
  created_by    Int?
  updated_by    Int?
  deleted_by    Int?
  city          city[]
}

model roles {
  id               Int                @id @default(autoincrement())
  name             String             @db.VarChar(255)
  is_active        Boolean            @default(true)
  created_at       DateTime           @default(now()) @db.DateTime()
  updated_at       DateTime?          @db.DateTime()
  deleted_at       DateTime?          @db.DateTime()
  created_by       Int?
  updated_by       Int?
  deleted_by       Int?
  role_permissions role_permissions[]
  users            users[]
}

model permissions {
  id        Int     @id @default(autoincrement())
  name      String  @db.VarChar(255)
  is_active Boolean @default(true)
  menu_id   Int

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  menus            menus              @relation(fields: [menu_id], references: [id])
  role_permissions role_permissions[]
}

model role_permissions {
  id Int @id @default(autoincrement())

  role_id       Int
  permission_id Int

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  roles       roles       @relation(fields: [role_id], references: [id])
  permissions permissions @relation(fields: [permission_id], references: [id])
}

model users {
  id        Int     @id @default(autoincrement())
  username  String  @db.VarChar(255)
  password  String  @db.VarChar(255)
  is_active Boolean @default(true)
  role_id   Int

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  roles    roles     @relation(fields: [role_id], references: [id])
  sales    sales[]
  employee employee?
  vendor   vendor[]
  tukang   tukang[]
}

model sales {
  id           Int       @id @default(autoincrement())
  store_id     Int?
  user_id      Int?
  full_name    String    @db.VarChar(255)
  nik          String    @db.VarChar(20)
  bank_id      Int?
  bank_branch  String    @db.VarChar(50)
  account_name String    @db.VarChar(255)
  is_active    Boolean   @default(true)
  created_at   DateTime  @default(now()) @db.DateTime()
  updated_at   DateTime? @db.DateTime()
  deleted_at   DateTime? @db.DateTime()
  created_by   Int?
  updated_by   Int?
  deleted_by   Int?
  store        store?    @relation(fields: [store_id], references: [id])
  users        users?    @relation(fields: [user_id], references: [id])
  bank         bank?     @relation(fields: [bank_id], references: [id])
  orders       orders[]
}

model service_type {
  id             Int              @id @default(autoincrement())
  service_type   String           @db.VarChar(45)
  is_active      Boolean          @default(true)
  created_at     DateTime         @default(now()) @db.DateTime()
  updated_at     DateTime?        @db.DateTime()
  deleted_at     DateTime?        @db.DateTime()
  created_by     Int?
  updated_by     Int?
  deleted_by     Int?
  vendor_service vendor_service[]
  tukang_service tukang_service[]
}

model store {
  id         Int         @id @default(autoincrement())
  store_name String      @db.VarChar(45)
  address    String      @db.VarChar(45)
  city_id    Int
  zip_code   String      @db.VarChar(45)
  created_at DateTime    @default(now()) @db.DateTime()
  updated_at DateTime?   @db.DateTime()
  deleted_at DateTime?   @db.DateTime()
  created_by Int?
  updated_by Int?
  deleted_by Int?
  sales      sales[]
  prices     prices[]
  employee   employee[]
  orders     orders[]
  quotation  quotation[]
}

model tukang {
  id        Int  @id @default(autoincrement())
  vendor_id Int?
  user_id   Int?

  full_name    String    @db.VarChar(45)
  address      String    @db.VarChar(45)
  phone_number String    @db.VarChar(45)
  ktp_number   String    @db.VarChar(45)
  email        String    @db.VarChar(45)
  bod          DateTime  @db.DateTime()
  join_date    DateTime? @default(now()) @db.DateTime()
  is_active    Boolean   @default(true)

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()
  created_by Int?
  updated_by Int?
  deleted_by Int?

  vendor          vendor?           @relation(fields: [vendor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users           users?            @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tukang_service  tukang_service[]
  orders          orders[]
  work_orders     work_orders[]
  tukang_document tukang_document[]
}

model tukang_document {
  id        Int @id @default(autoincrement())
  tukang_id Int

  document_name String @db.VarChar(255)
  path          String @db.VarChar(255)

  is_active Boolean @default(true)

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  tukang tukang @relation(fields: [tukang_id], references: [id])
}

model tukang_service {
  id              Int  @id @default(autoincrement())
  tukang_id       Int?
  service_type_id Int?

  is_active Boolean @default(true)

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  created_by   Int?
  updated_by   Int?
  deleted_by   Int?
  tukang       tukang?       @relation(fields: [tukang_id], references: [id])
  service_type service_type? @relation(fields: [service_type_id], references: [id])
}

model vendor {
  id            Int       @id @default(autoincrement())
  user_id       Int?
  company_name  String    @db.VarChar(45)
  address       String    @db.VarChar(200)
  phone_number  String    @db.VarChar(30)
  ktp_number    String?
  npwp_number   String?
  email_address String    @db.VarChar(100)
  join_date     DateTime? @default(now()) @db.DateTime()
  is_active     Boolean   @default(true)

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  created_by      Int?
  updated_by      Int?
  deleted_by      Int?
  tukang          tukang[]
  vendor_area     vendor_area[]
  users           users?            @relation(fields: [user_id], references: [id])
  vendor_bank     vendor_bank[]
  vendor_document vendor_document[]
  vendor_service  vendor_service[]
  orders          orders[]
  work_orders     work_orders[]
}

model vendor_area {
  id               Int       @id @default(autoincrement())
  vendor_id        Int?
  city_id          Int?
  default_discount Decimal   @db.Decimal(2, 0)
  default_markup   String    @db.VarChar(45)
  default_unit     String    @default("unit") @db.VarChar(45)
  is_active        Boolean   @default(true)
  created_at       DateTime  @default(now()) @db.DateTime()
  updated_at       DateTime? @db.DateTime()
  deleted_at       DateTime? @db.DateTime()
  created_by       Int?
  updated_by       Int?
  deleted_by       Int?
  vendor           vendor?   @relation(fields: [vendor_id], references: [id])
  city             city?     @relation(fields: [city_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model vendor_bank {
  id             Int       @id @default(autoincrement())
  vendor_id      Int?
  bank_id        Int?
  account_name   String    @db.VarChar(255)
  account_number String    @db.VarChar(50)
  is_active      Boolean   @default(true)
  created_at     DateTime  @default(now()) @db.DateTime()
  updated_at     DateTime? @db.DateTime()
  deleted_at     DateTime? @db.DateTime()
  created_by     Int?
  updated_by     Int?
  deleted_by     Int?
  vendor         vendor?   @relation(fields: [vendor_id], references: [id])
  bank           bank?     @relation(fields: [bank_id], references: [id])
}

model vendor_document {
  id            Int       @id @default(autoincrement())
  vendor_id     Int?
  document_name String    @db.VarChar(255)
  path          String    @db.VarChar(255)
  is_active     Boolean   @default(true)
  created_at    DateTime  @default(now()) @db.DateTime()
  updated_at    DateTime? @db.DateTime()
  deleted_at    DateTime? @db.DateTime()
  created_by    Int?
  updated_by    Int?
  deleted_by    Int?
  vendor        vendor?   @relation(fields: [vendor_id], references: [id])
}

model vendor_service {
  id              Int           @id @default(autoincrement())
  vendor_id       Int?
  service_type_id Int?
  is_active       Boolean       @default(true)
  created_at      DateTime      @default(now()) @db.DateTime()
  updated_at      DateTime?     @db.DateTime()
  deleted_at      DateTime?     @db.DateTime()
  created_by      Int?
  updated_by      Int?
  deleted_by      Int?
  vendor          vendor?       @relation(fields: [vendor_id], references: [id])
  service_type    service_type? @relation(fields: [service_type_id], references: [id])
}

model status {
  id              Int               @id @default(autoincrement())
  category        String
  description     String
  created_at      DateTime          @default(now()) @db.DateTime()
  updated_at      DateTime?
  order           orders[]
  m_order_details m_order_details[]
  complaints      complaints[]
  work_orders     work_orders[]
  quotation       quotation[]
  remedials       remedials[]
  refund          refund[]
}

model orders {
  id                Int  @id @default(autoincrement())
  member_id         Int
  seles_id          Int
  store_id          Int
  project_status_id Int
  vendor_id         Int?
  tukang_id         Int?
  // category_id       Int?

  project_address         String
  project_number          String  @default("0")
  receipt_number          String? @db.NVarChar(30)
  receipt_path            String? @db.Text
  total_estimate_workdays Int     @db.TinyInt
  payment_type            String  @db.VarChar(200)
  grand_total             Decimal @db.Decimal(32, 2)
  grand_total_comission   Decimal @db.Decimal(32, 2)
  print_counter           Int     @db.TinyInt()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  members         members?          @relation(fields: [member_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  sales           sales?            @relation(fields: [seles_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vendor          vendor?           @relation(fields: [vendor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  tukang          tukang?           @relation(fields: [tukang_id], references: [id])
  status          status?           @relation(fields: [project_status_id], references: [id])
  // categories      categories?       @relation(fields: [category_id], references: [id])
  store           store?            @relation(fields: [store_id], references: [id])
  m_order_details m_order_details[]
  complaints      complaints[]
  work_orders     work_orders[]
  invoices        invoices[]
  quotation       quotation[]
  refund          refund[]
}

model m_order_details {
  id              Int @id @default(autoincrement())
  order_id        Int
  item_id         Int
  order_status_id Int

  unit         String  @db.VarChar(200)
  unit_price   Decimal @db.Decimal(32, 2)
  quote_price  Decimal @db.Decimal(32, 2)
  quantity     Decimal @db.Decimal(32, 2)
  total        Int
  survey_price Decimal @db.Decimal(32, 2)
  comission    Int

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  order  orders @relation(fields: [order_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  item   items  @relation(fields: [item_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  status status @relation(fields: [order_status_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model prices {
  id               Int      @id @default(autoincrement())
  item_id          Int
  unit_id          Int
  store_id         Int?
  periodic_start   DateTime
  periodic_end     DateTime
  nominal_discount String   @db.VarChar(50)
  price            Int
  is_active        Boolean  @default(true)

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  store store? @relation(fields: [store_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  units units  @relation(fields: [unit_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  items items  @relation(fields: [item_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model units {
  id        Int     @id @default(autoincrement())
  unit_name String  @db.VarChar(200)
  is_active Boolean @default(true)

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()
  prices     prices[]
}

model categories {
  id            Int    @id @default(autoincrement())
  category_name String @db.VarChar(200)

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()
  // orders     orders[]
}

model quotation {
  id Int @id @default(autoincrement())

  store_id Int @db.Int
  order_id Int @db.Int

  description        String   @db.Text()
  quotation_number   String
  quotation_date     DateTime @db.Date()
  quotation_validity DateTime @db.Date()
  quotation_status   Int

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at      DateTime          @default(now()) @db.DateTime()
  updated_at      DateTime?         @db.DateTime()
  deleted_at      DateTime?         @db.DateTime()
  store           store             @relation(fields: [store_id], references: [id])
  order           orders            @relation(fields: [order_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  status          status            @relation(fields: [quotation_status], references: [id])
  quotation_files quotation_files[]
}

model quotation_files {
  id Int @id @default(autoincrement())

  quotation_id Int
  path         String @db.Text()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  quotation quotation @relation(fields: [quotation_id], references: [id])
}

model complaint_channels {
  id Int @id @default(autoincrement())

  name String @db.Text()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime     @default(now()) @db.DateTime()
  updated_at DateTime?    @db.DateTime()
  deleted_at DateTime?    @db.DateTime()
  complaints complaints[]
}

model complaints {
  id Int @id @default(autoincrement())

  order_id          Int
  complaint_channel Int?
  complaint_status  Int

  description    String   @db.Text()
  complaint_date DateTime @db.Date()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  orders             orders               @relation(fields: [order_id], references: [id])
  status             status?              @relation(fields: [complaint_status], references: [id], onDelete: NoAction, onUpdate: NoAction)
  complaint_evidence complaint_evidence[]
  remedials          remedials[]
  complaint_channels complaint_channels?  @relation(fields: [complaint_channel], references: [id])
}

model complaint_evidence {
  id Int @id @default(autoincrement())

  complaint_id      Int
  evidence_location String @db.Text()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  complaints complaints @relation(fields: [complaint_id], references: [id])
}

model remedials {
  id Int @id @default(autoincrement())

  complaint_id    Int
  remedial_action String
  ra_date_start   DateTime @db.Date()
  ra_date_end     DateTime @db.Date()
  remedial_pic    Int?
  remedial_status Int?

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  complaints         complaints?          @relation(fields: [complaint_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  status             status?              @relation(fields: [remedial_status], references: [id], onDelete: NoAction, onUpdate: NoAction)
  remedial_evidences remedial_evidences[]
}

model remedial_evidences {
  id Int @id @default(autoincrement())

  remedial_id       Int
  evidence_location String

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  remedials remedials @relation(fields: [remedial_id], references: [id])
}

model work_orders {
  id        Int @id @default(autoincrement())
  order_id  Int
  tukang_id Int
  vendor_id Int

  worker_order_status Int
  request_work_time   DateTime @db.DateTime()
  survey_date         DateTime @db.Date()
  work_start_date     DateTime @db.Date()
  work_end_date       DateTime @db.Date()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at     DateTime         @default(now()) @db.DateTime()
  updated_at     DateTime?        @db.DateTime()
  deleted_at     DateTime?        @db.DateTime()
  status         status?          @relation(fields: [worker_order_status], references: [id], onDelete: NoAction, onUpdate: NoAction)
  order          orders           @relation(fields: [order_id], references: [id])
  work_evidences work_evidences[]
  tukang         tukang           @relation(fields: [tukang_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  vendor         vendor           @relation(fields: [vendor_id], references: [id])
}

model work_evidences {
  id Int @id @default(autoincrement())

  work_order_id     Int
  evidence_location String

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  work_orders work_orders @relation(fields: [work_order_id], references: [id])
}

model invoices {
  id Int @id @default(autoincrement())

  order_id          Int
  request_work_time DateTime @db.DateTime()
  survey_date       DateTime @db.Date()
  work_start_date   DateTime @db.Date()
  work_end_date     DateTime @db.Date()

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()
  order      orders    @relation(fields: [order_id], references: [id])
}

model refund {
  id Int @id @default(autoincrement())

  order_id      Int
  refund_status Int

  notes           String   @db.Text()
  reason          String   @db.Text()
  approval_number String   @db.VarChar(45)
  voucher         String?  @db.VarChar(45)
  penalty_nominal Decimal? @db.Decimal(32, 2)
  date_approve    DateTime @db.DateTime
  date_of_filing  DateTime @db.DateTime

  created_by Int?
  updated_by Int?
  deleted_by Int?

  created_at DateTime  @default(now()) @db.DateTime()
  updated_at DateTime? @db.DateTime()
  deleted_at DateTime? @db.DateTime()

  orders orders  @relation(fields: [order_id], references: [id])
  status status? @relation(fields: [refund_status], references: [id], onDelete: NoAction, onUpdate: NoAction)
}
